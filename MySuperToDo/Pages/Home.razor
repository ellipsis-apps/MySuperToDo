@page "/"
@inject NavigationManager Navigation

@code {
    protected override void OnInitialized()
    {
        // Login page removed: redirect root to lists now that deterministic seeds are used.
        Navigation.NavigateTo("lists", replace: true);
    }
}
